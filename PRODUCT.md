# Product

<!-- impeccable:product-schema 1 -->

## Platform

adaptive

## Users

主要用户是在同一局域网内使用 Windows 电脑与 Android 手机传输文字、文件和安装包的个人用户。典型场景包括桌面办公、手机与电脑互传、手机之间分享，以及 VPN 开启时仍需要局域网通信。

## Product Purpose

LocalLink 让同一局域网中的设备不经过互联网即可发现彼此、建立可信连接并传输内容。成功意味着用户能够迅速确认本机服务状态、找到目标设备、完成发送，并在记录中继续预览、打开或管理收到的内容。

## Positioning

LocalLink 的差异化机制是每台 Windows 或 Android 设备都可以作为独立局域网主机运行，在稳定设备身份、配对码和本地可信记录的约束下完成点对点传输，而不依赖云账号或中心服务器。

## Operating Context

产品运行于家庭、宿舍或办公室 Wi-Fi。Windows 端以 EXE 和内嵌网页控制台工作；Android 端包含原生设备入口和本地 WebView 收件箱。设备可能休眠、重启、更换 DHCP 地址或启用 VPN，因此服务状态、当前地址、可信身份和可恢复错误必须清晰可见。

## Capabilities and Constraints

- 设备发现、手动 IP 连接、二维码连接和可信设备快捷入口。
- 文字与文件发送、传输记录、搜索、筛选、预览、下载、打开、安装和删除。
- Windows 与 Android 都可作为接收主机，默认端口为 53317。
- 局域网传输不等于端到端加密；配对码和稳定设备 ID 负责当前版本的访问控制与身份连续性。
- UI 必须在离线、检测、认证、重新信任、发送、成功和失败状态下提供明确恢复路径。

## Brand Commitments

保留 LocalLink 名称、现有蓝绿连接图标和“本地直连、无需互联网”的产品语义。用户明确要求双端统一的现代科技感、适量背景动态效果、组件微交互和不干扰使用的反馈音效。

## Evidence on Hand

- 应用图标与现有静态资源位于 `assets/`、`locallink/static/` 和 Android 资源目录。
- 当前功能与交互事实由 `README.md`、`protocol/protocol.md` 和现有实现提供。
- 没有可引用的商业数据、客户评价或性能宣传，界面不得虚构这些内容。

## Product Principles

- 先呈现连接状态与下一步操作，再呈现装饰信息。
- 信任必须可理解、可恢复，但不能静默降低安全边界。
- 跨端保持同一品牌语法，同时遵循 Windows 桌面和 Android 原生操作习惯。
- 动效和声音用于确认发现、连接与传输结果，不持续争夺注意力。
- 收到的内容留在本地，并让用户随时清楚它来自谁、保存在哪里、可以执行什么操作。

## Accessibility & Inclusion

界面需要支持键盘焦点、屏幕阅读语义、至少 48dp 的 Android 触控目标、文本缩放、足够对比度，以及系统减少动画偏好。声音不能作为唯一反馈，并应允许关闭。
