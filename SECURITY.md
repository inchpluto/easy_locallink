# 安全策略

LocalLink 是一个局域网设备互传工具，涉及网络通信、配对码认证与文件接收。我们非常重视安全问题。

## 支持的版本

| 版本 | 支持情况 |
| ---- | -------- |
| 2.4.x | 支持（当前版本 2.4.4） |
| < 2.4 | 不再支持 |

## 报告漏洞

如果你发现安全漏洞，请**不要**在公开 issue、PR 或讨论中披露，以免在修复前暴露给攻击者。

请通过以下方式私下报告：

1. 在 GitHub 上使用「Report a vulnerability」或私下联系维护者。
2. 报告中请尽量包含：
   - 受影响的版本和平台（Windows / Android）
   - 漏洞类型和影响
   - 复现步骤或概念验证（PoC）
   - 建议的修复方向（如有）

## 已知安全边界

请先阅读 [protocol/protocol.md](protocol/protocol.md)，其中说明了协议细节与安全边界。需要特别注意的是：

- **局域网传输不等于端到端加密。** 当前版本的访问控制依赖配对码与稳定设备 ID，而不是加密传输。
- 配对码是进程级临时凭证，默认值为 `123456`，仅用于个人设备在同一局域网内的信任建立，不应视为强认证。
- 请在受信任的网络（如家庭/办公 Wi-Fi）中使用；避免在公共、不可信网络下开启服务。

## 处理流程

1. 确认报告后，我们会在合理时间内响应。
2. 修复前，漏洞细节不会被公开。
3. 修复发布后，我们会公开致谢（除非你要求匿名）。
